// import { navRoutes } from "../../constants";
// import { Link } from "react-router-dom";

const Navbar = () => {
	return (
		<>
			{/* <ul>
				<p>loop</p>
				{
					navRoutes().map(route => {
						return <li key={route.num}>
						<Link to={'/'} >home</Link>
					</li>
					})
				}
			</ul> */}
		</>
	);
};

export default Navbar;